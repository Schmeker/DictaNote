name: Dictanote
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  postgres: ^3.5.6
  group_radio_button: ^1.3.0
  flutter:
    sdk: flutter

  # Rich-Text Editor
  flutter_quill: ^11.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/dictanote.png
